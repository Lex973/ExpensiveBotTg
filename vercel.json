{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["iad1"],
  "functions": {
    "app.py": {
      "maxDuration": 60,
      "excludeFiles": "{tests/**,data/**,deploy/**,scripts/**,*.zip,*.tar.gz}"
    }
  }
}
